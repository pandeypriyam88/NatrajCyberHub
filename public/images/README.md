Drop hero photos here (e.g. ticket-booking.jpg, printing-xerox.jpg,
documentation.jpg, online-forms.jpg), then set the matching `src` field
in `src/data/heroImages.ts` to `/images/your-file-name.jpg`.

Until a photo is added, that tile automatically falls back to a simple
icon + label — nothing breaks either way.
